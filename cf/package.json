{
  "name": "cinehall-cf",
  "private": true,
  "version": "1.0.0",
  "description": "CineHall Cloudflare Worker deploy tooling",
  "scripts": {
    "deploy": "CF_TOKEN=$CF_TOKEN bash deploy.sh"
  },
  "devDependencies": {
    "wrangler": "^4.120.0"
  }
}
